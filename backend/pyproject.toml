[project]
name = "ai-copilot-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi", "uvicorn[standard]", "pydantic-settings", "sqlalchemy[asyncio]", "asyncpg", "alembic", "redis", "httpx", "PyJWT", "pwdlib[argon2]", "python-multipart", "pypdf"]
